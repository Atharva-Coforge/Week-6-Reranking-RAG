[tool.ruff]
target-version = "py312"
extend-exclude = ["data"]

[tool.mypy]
python_version = "3.12"
ignore_missing_imports = true
exclude = ["(?x)(^data/)"]

[tool.pytest.ini_options]
testpaths = ["tests"]
